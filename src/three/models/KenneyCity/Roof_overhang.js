/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useRef, useMemo } from 'react'
import { useGLTF, Merged } from '@react-three/drei'

export default function InstancedModel(props) {
  const { nodes } = useGLTF('/models/kenney_city/roof_overhang.glb')
  const instances = useMemo(
    () => ({
      Meshroofoverhang: nodes.Mesh_roof_overhang,
      Meshroofoverhang1: nodes.Mesh_roof_overhang_1,
    }),
    [nodes]
  )
  return (
    <Merged meshes={instances} {...props}>
      {(instances) => <Model instances={instances} />}
    </Merged>
  )
}

function Model({ instances, ...props }) {
  const group = useRef()
  const { nodes, materials } = useGLTF('/models/kenney_city/roof_overhang.glb')
  return (
    <group ref={group} {...props} dispose={null}>
      <instances.Meshroofoverhang />
      <instances.Meshroofoverhang1 />
    </group>
  )
}

useGLTF.preload('/models/kenney_city/roof_overhang.glb')
