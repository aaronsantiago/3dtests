/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useRef, useMemo } from 'react'
import { useGLTF, Merged } from '@react-three/drei'

export default function InstancedModel(props) {
  const { nodes } = useGLTF('/models/kenney_city/low_wideA.glb')
  const instances = useMemo(
    () => ({
      MeshlowwideA: nodes.Mesh_low_wideA,
      MeshlowwideA1: nodes.Mesh_low_wideA_1,
    }),
    [nodes]
  )
  return (
    <Merged meshes={instances} {...props}>
      {(instances) => <Model instances={instances} />}
    </Merged>
  )
}

function Model({ instances, ...props }) {
  const group = useRef()
  const { nodes, materials } = useGLTF('/models/kenney_city/low_wideA.glb')
  return (
    <group ref={group} {...props} dispose={null}>
      <instances.MeshlowwideA />
      <instances.MeshlowwideA1 />
    </group>
  )
}

useGLTF.preload('/models/kenney_city/low_wideA.glb')
