/*
Auto-generated by: https://github.com/pmndrs/gltfjsx
*/

import React, { useRef, useMemo } from 'react'
import { useGLTF, Merged } from '@react-three/drei'

export default function InstancedModel(props) {
  const { nodes } = useGLTF('/models/kenney_city/roof_center.glb')
  const instances = useMemo(
    () => ({
      Roofcenter: nodes.roof_center,
    }),
    [nodes]
  )
  return (
    <Merged meshes={instances} {...props}>
      {(instances) => <Model instances={instances} />}
    </Merged>
  )
}

function Model({ instances, ...props }) {
  const group = useRef()
  const { nodes, materials } = useGLTF('/models/kenney_city/roof_center.glb')
  return (
    <group ref={group} {...props} dispose={null}>
      <instances.Roofcenter />
    </group>
  )
}

useGLTF.preload('/models/kenney_city/roof_center.glb')
